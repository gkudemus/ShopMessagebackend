{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "mock api server for shop message",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch db.json",
    "client": "npm start --prefix my-app",
    "dev": "concurrently \"npm run json:server\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0",
    "json-server": "^0.14.2"
  }
}
